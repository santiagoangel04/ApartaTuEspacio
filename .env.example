# URL del servicio donde se guardarán los registros (Formspree, Google Apps Script,
# Supabase Edge Function, API propia, etc.). Recibe un POST con JSON:
# { name, phone, email, consent, source, createdAt }
# Si se deja vacía, el formulario simula el envío (modo demo).
VITE_LEADS_ENDPOINT=
